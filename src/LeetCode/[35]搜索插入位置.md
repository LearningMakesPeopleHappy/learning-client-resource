# 35. 搜索插入位置

## 题目

给定一个排序数组和一个目标值，在数组中找到目标值，并返回其索引。如果目标值不存在于数组中，返回它将会被按顺序插入的位置。

你可以假设数组中无重复元素。

> **测试**
>
> [1,3,5,6], 5 - 2
>
> [1,3,5,6], 2 - 1
>
> [1,3,5,6], 7 - 4
>
> [1,3,5,6], 0 - 0

## 思路

### 顺序遍历



#### 时间复杂度



#### 空间复杂度



#### 代码

```javascript
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var searchInsert = function(nums, target) {
    let index = -1;
    const len = nums.length;
    for (let i = 0; i < len; i++) {
        if (nums[i] === target) return i;
        if (nums[i] > target && index < 0) index = i;
    }

    if (index < 0) return len;
    return index;
};
```



### 二分查找法



#### 时间复杂度



#### 空间复杂度



#### 代码

```javascript
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var searchInsert = function(nums, target) {
    const len = nums.length;
    if (len) {
        let i = Math.floor(len / 2);
        const center = nums[i];
        if (center === target) {
            return i;
        } else if (center > target) {
            if (len > 1) {
                return searchInsert(nums.slice(0, i), target);
            } else {
                return 0;
            }
        } else {
            if (len > 2) {
                return i + searchInsert(nums.slice(i + 1), target) + 1;
            } else {
                return i + 1;
            }
        }
    }
    
    return 0;
};
```

